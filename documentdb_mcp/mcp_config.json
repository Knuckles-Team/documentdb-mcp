{
  "mcpServers": {
    "documentdb": {
      "command": "documentdb-mcp",
      "args": ["--transport", "stdio"],
      "env": {
        "DOCUMENTDB_URL": "${DOCUMENTDB_URL:-http://localhost:8080}",
        "DOCUMENTDB_TOKEN": "${DOCUMENTDB_TOKEN}"
      }
    }
  }
}
